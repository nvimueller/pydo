[build-system]
requires = ["setuptools>=61.0","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pydo"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
pydo = "src.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
